TODO:

D User can see empty chat page
D User gets assigned a id which is remembered using a long lived cookie
D User gets assigned a color based on the least recently used
D User can view chat
D New user gets announced in chat
D User can add a chat message, clicking or pressing return to submit
D User can see what other users are typing
* User see announcement when others leave chat
* User can signup with a username and password
* User can login
* Users see their and others usernames

D Fix race bug on startup
D improved client code structure
* Fix scroll, so latest message always visible
* In client, make view list for events, app emit events



